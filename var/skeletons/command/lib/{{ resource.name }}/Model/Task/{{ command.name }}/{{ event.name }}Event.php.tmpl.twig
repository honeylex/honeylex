<?php

namespace {{ crate.vendor }}\{{ crate.name }}\{{ resource.name }}\Model\Task\{{ command.name }};

use {{ parent.namespace }}\{{ parent.event }};

class {{ event.name }}Event extends {{ parent.event }}
{
}
