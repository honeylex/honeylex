subscriptions:
  - transport: sync
    commands:
      {{ crate.vendor|snake }}.{{ crate.name|snake }}.{{ resource.name|snake }}.proceed_{{ resource.name|snake }}_workflow:
        handler: {{ crate.vendor }}\{{ crate.name }}\{{ resource.name }}\Model\Task\Proceed{{ resource.name }}Workflow\Proceed{{ resource.name }}WorkflowCommandHandler