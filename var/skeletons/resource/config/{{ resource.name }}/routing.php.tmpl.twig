<?php

// everything in here will be mounted by default below the prefix '/{{ crate.vendor|snake }}/{{ crate.name|snake }}/{{ resource.name|snake }}'

use {{ crate.vendor }}\{{ crate.name }}\{{ resource.name }}\Controller\IndexController;

$routing->mount('/{{ resource.name|snake }}', function ($routing) {
    $routing->get('/', [ IndexController::CLASS, 'read' ])->bind($this->getPrefix().'.{{ resource.name|snake }}.index');
});
