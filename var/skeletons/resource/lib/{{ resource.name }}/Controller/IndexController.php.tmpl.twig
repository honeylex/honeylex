<?php

namespace {{ crate.namespace }}\{{ resource.name }}\Controller;

use Honeylex\Renderer\TemplateRendererInterface;

class IndexController
{
    protected $templateRenderer;

    public function __construct(TemplateRendererInterface $templateRenderer)
    {
        $this->templateRenderer = $templateRenderer;
    }

    public function read()
    {
        return $this->templateRenderer->render('@{{ crate.vendor|snake }}-{{ crate.name|snake }}/{{ resource.name|snake }}/index.html.twig', [ 'title' => __METHOD__ ]);
    }
}
