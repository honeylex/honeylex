<?php

// everything in here will be mounted below the prefix '/{{ crate.prefix }}'

use {{ crate.namespace }}\Controller\IndexController;

$routing->get('/', [ IndexController::CLASS, 'read' ])->bind('{{ crate.prefix }}.index');
